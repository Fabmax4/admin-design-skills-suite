# 输出模板

## 设计任务

- `任务阶段`：
- `建议调用的 Skill`：
- `调用顺序`：
- `本轮重点`：
- `暂不进入的层`：
- `下一步`：

## 评审任务

- `审查对象`：
- `先检查的层`：
- `重点风险`：
- `是否立即进入 admin-design-review`：
- `修复优先级`：

## 规则

- 输出优先说明为什么从这一层开始
- 明确哪些层本轮不进入
- 不输出冗长路线图，只给当前最小有效链路
